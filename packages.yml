packages:
  - package: dbt-labs/dbt_utils
    version: [">=1.0.0", "<2.0.0"]

  - package: dbt-labs/audit_helper
    version: [">=0.9.0", "<1.0.0"]

  - package: calogica/dbt_expectations
    version: [">=0.10.0", "<1.0.0"]

  # elementary-data/elementary: incompatible with local DuckDB (SQL syntax issue)
  # Enable when using a cloud warehouse (Snowflake, BigQuery, Redshift)
  # - package: elementary-data/elementary
  #   version: [">=0.14.0", "<1.0.0"]
